#!/bin/bash
set -e

ip addr add 10.0.0.1/24 dev eth0
ip link set eth0 up

echo "nameserver 1.1.1.1" >/etc/resolv.conf

export LD_PRELOAD=/opt/liblzma-patched/liblzma.so.5

/usr/sbin/sshd -d -p 2222 &
