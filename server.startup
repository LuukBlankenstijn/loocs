#!/bin/bash
set -e

ip addr add 10.0.0.1/24 dev eth0
ip link set eth0 up

echo "nameserver 1.1.1.1" >/etc/resolv.conf

useradd -r -s /usr/sbin/nologin -d /var/empty/sshd -c "sshd privsep" sshd
/usr/sbin/sshd -d &
